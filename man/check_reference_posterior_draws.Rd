% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_pdb_bundle.R,
%   R/utils_reference_posterior.R
\name{check_reference_posterior_draws.pdb_reference_bundle}
\alias{check_reference_posterior_draws.pdb_reference_bundle}
\alias{check_reference_posterior_draws}
\alias{check_reference_posterior_draws.character}
\alias{check_reference_posterior_draws.pdb_posterior}
\alias{check_reference_posterior_draws.pdb_reference_posterior_draws}
\title{Check that reference posterior draws follows the
reference posterior draws definition.}
\usage{
\method{check_reference_posterior_draws}{pdb_reference_bundle}(x, ...)

check_reference_posterior_draws(x, ...)

\method{check_reference_posterior_draws}{character}(x, ...)

\method{check_reference_posterior_draws}{pdb_posterior}(x, ...)

\method{check_reference_posterior_draws}{pdb_reference_posterior_draws}(x, ...)
}
\arguments{
\item{x}{a posterior name, posterior object, reference-posterior draws, or
a `pdb_reference_bundle` returned by [create_pdb_bundle()].}

\item{...}{currently not used.}
}
\value{
For a `pdb_reference_bundle`, the updated bundle with a full
  diagnostic report and acceptance flags attached when all required checks
  pass. Other supported objects retain their existing return behavior.
}
\description{
Check that reference posterior draws follows the
reference posterior draws definition.
}
\details{
Requires exactly 10,000 retained draws. For Stan sampling,
  the recorded diagnostics must describe the draws and show at least four
  chains, mean absolute lag-1 autocorrelation across chains at most 0.05
  for every variable, R-hat at most 1.01, E-FMI at least 0.2 in every
  chain, and no divergent transitions. ESS bounds are recorded but do not
  determine acceptance.
}
